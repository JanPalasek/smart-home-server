@model SmartHome.Web.Models.Place.PlaceViewModel
@{
    ViewBag.Header = "Place";
}

<div asp-validation-summary="All"></div>

<section class="section control-section form-section">
    <form method="post">
        <input type="hidden" asp-for="Model.Id"/>
        <div class="form-group">
            <ejs-textbox id="name" ejs-for="Model.Name" placeholder="Name" floatLabelType="Auto"></ejs-textbox>
        </div>
        <div class="form-group">
            <ejs-checkbox id="is-inside" ejs-for="Model.IsInside" label="Inside"></ejs-checkbox>
        </div>
        <div class="form-group">
            <ejs-textbox ejs-for="Model.Note" id="note" placeholder="Note" multiline="true" floatLabelType="Auto"></ejs-textbox>
        </div>
        <ejs-button id="button" type="submit" content="Submit"></ejs-button>
    </form>
</section>